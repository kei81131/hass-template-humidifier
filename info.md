# Humidifier Template

Create a template-based Home Assistant humidifier entity from existing entities,
sensors, and service calls.

After installing, restart Home Assistant and configure it in YAML under the
`humidifier:` integration.
